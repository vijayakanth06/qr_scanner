{
  "rules": {
    "colleges": {
      "$collegeId": {
        ".read": true,
        ".write": "auth != null && root.child('admins').child(auth.uid).child('collegeId').val() === $collegeId"
      }
    },
    "admins": {
      ".read": "auth != null",
      ".write": false
    }
  }
}
